#!/usr/bin/env python -S

from sys import argv, exit
from math import sin, cos, asin, sqrt, radians

if len(argv) != 5:
  print("Usage: distance lat1 lon1 lat2 lon2")
  exit(1)

lat1 = float(argv[1].rstrip().rstrip(','))
lon1 = float(argv[2].rstrip().rstrip(','))
lat2 = float(argv[3].rstrip().rstrip(','))
lon2 = float(argv[4].rstrip().rstrip(','))

dLat = radians(lat2 - lat1)
dLon = radians(lon2 - lon1)

print(6372.8 * 2 * asin(sqrt(sin(dLat / 2)**2 + sin(dLon / 2)**2 * cos(radians(lat1)) * cos(radians(lat2)))))
